## How to add new resources?

- open locally `stats_resources.csv` using excel and add resources, save changes
- change a small thing in the index.qmd file (is necessary that GitHub pages recogniezes changes)
- render `index.qmd` to `index.html`
- commit and push changes in `index.qmd` and `index.html` file to remote repo
